import { Extension } from '@codemirror/state'

// Task 6 stub: Overleaf's command-tooltip pulls in split-test / event-tracking
// and a large React tooltip stack. The visual editor only needs an Extension
// here, so we provide an empty one. TODO: port full command tooltip later.
export const commandTooltip: Extension = []
